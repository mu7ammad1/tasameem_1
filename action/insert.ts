// import { createClient } from "@/utils/supabase/server";

// const supabase = createClient();
// const {
//   data: { user },
// } = await supabase.auth.getUser();

// const { data, error } = await supabase
//   .from("profiles")
//   .insert([{ id: user?.id, username: "someValue", full_name: `user?.id` }])
//   .select();
